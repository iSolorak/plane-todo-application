{
  "name": "@plane-todo/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "Expo/React Native client for Plane work items.",
  "main": "index.ts",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "typecheck": "tsc -p tsconfig.verify.json --noEmit",
    "typecheck:app": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@plane-todo/core": "workspace:*",
    "@tanstack/react-query": "^5.51.0",
    "expo": "~51.0.0",
    "expo-status-bar": "~1.12.0",
    "react": "18.2.0",
    "react-native": "0.74.5"
  },
  "devDependencies": {
    "@types/react": "~18.2.79",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
