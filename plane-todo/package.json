{
  "name": "plane-todo",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev:notifier": "pnpm --filter @plane-todo/notifier dev",
    "dev:core": "pnpm --filter @plane-todo/core dev",
    "start:notifier": "pnpm --filter @plane-todo/notifier start"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
